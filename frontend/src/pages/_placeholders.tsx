/** Destino post-login de Docente/Estudiante hasta que existan sus pantallas propias. */
export function InicioPlaceholder() {
  return <p>Sesión iniciada — pendiente de pantalla propia</p>
}

/** Detalle de una cuenta hasta que `US-2.2.7` reemplace este placeholder. */
export function CuentaDetallePlaceholder() {
  return <p>Detalle de cuenta — pendiente de pantalla propia (US-2.2.7)</p>
}
