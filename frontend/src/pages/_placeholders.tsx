/** Destino post-login de Docente/Estudiante hasta que existan sus pantallas propias. */
export function InicioPlaceholder() {
  return <p>Sesión iniciada — pendiente de pantalla propia</p>
}

/** Destino temporal de las rutas del BC Banco de Preguntas hasta que US-2.1.9 a US-2.1.13 las reemplacen. */
export function BancoPreguntasPlaceholder() {
  return <p>Banco de Preguntas — pendiente de pantalla propia</p>
}
